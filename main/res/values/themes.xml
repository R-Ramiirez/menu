<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#FFFFFF</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarTheme">@style/ThemeOverlay.AppTheme.ActionBar</item>
        <item name="actionBarStyle">@style/Widget.AppTheme.ActionBar</item>
    </style>

    <style name="ThemeOverlay.AppTheme.ActionBar" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/black</item>
    </style>

    <style name="Widget.AppTheme.ActionBar" parent="Widget.MaterialComponents.ActionBar.Solid">
        <item name="titleTextStyle">@style/TextAppearance.AppTheme.ActionBar.Title</item>
    </style>

    <style name="TextAppearance.AppTheme.ActionBar.Title" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Estilo para los botones de MainActivity -->
    <style name="MainActivityButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">18sp</item>
        <item name="android:paddingEnd">5dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:paddingStart">5dp</item>
        <item name="android:gravity">start|center_vertical</item>
        <item name="backgroundTint">#FFFFFF</item>
        <item name="cornerRadius">50dp</item>
        <item name="android:drawablePadding">10dp</item>
    </style>

    <!-- Estilo para los botones de StartActivity -->
    <style name="StartActivityButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">#4C2555</item>
        <item name="android:textSize">18sp</item>
        <item name="backgroundTint">#61C4B6</item>
        <item name="cornerRadius">30dp</item>
        <item name="elevation">0dp</item> <!-- Quitamos la elevación nativa -->
        <item name="android:stateListAnimator">@null</item>
    </style>

    <!-- Estilo para Action bar -->
    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">#BAFF3B</item> <!-- O el color que prefieras -->
        <item name="android:titleTextStyle">@style/MyActionBarTitle</item>
    </style>

    <style name="MyActionBarTitle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/black</item>
    </style>
</resources>